# application.properties
spring.application.name=hrms

spring.profiles.active=${SPRING_PROFILES_ACTIVE:dev}

# ===============================
# POSTGRE DATABASE (LOCAL)
# ===============================
spring.datasource.url=jdbc:postgresql://localhost:5432/gm_hrms_db
spring.datasource.username=postgres
spring.datasource.password=gmt2025
spring.datasource.driver-class-name=org.postgresql.Driver

# ===============================
# MYSQL DATABASE
# ===============================
#spring.datasource.url=jdbc:mysql://localhost:3306/gm_hrms_db?useSSL=false&serverTimezone=UTC
#spring.datasource.username=root
#spring.datasource.password=2409
#spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver


# ===============================
# HIBERNATE
# ===============================
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
#spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true

# ===============================
# SERVER (RAILWAY)
# ===============================
server.port=${PORT:8080}

# ===============================
# DEV TOOLS
# ===============================
spring.devtools.restart.enabled=true
spring.devtools.livereload.enabled=false

jwt.secret=mF8YpQ2zVt9xLk7rN4cHj3WbS1eR6uTaZ0yXvBnPdQs=
jwt.access-token-expiration=900000
jwt.refresh-token-expiration=604800000

spring.mvc.throw-exception-if-no-handler-found=true
spring.web.resources.add-mappings=false


spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=2
spring.datasource.hikari.idle-timeout=30000

server.tomcat.max-part-count=-1
# ===============================
# LOGGING
# ===============================
logging.config=classpath:logback-spring.xml

logging.level.root=WARN
logging.level.com.gm.hrms=INFO

logging.level.org.hibernate.SQL=WARN
logging.level.org.hibernate.orm.jdbc.bind=WARN

# ===============================
# AOP (make sure it's enabled)
# ===============================
spring.aop.auto=true
spring.aop.proxy-target-class=true

# Async
spring.task.execution.pool.core-size=2
spring.task.execution.pool.max-size=5
spring.task.execution.thread-name-prefix=email-

cors.allowed-origins=http://localhost:5173,http://192.168.1.15:5173

app.file-upload.base-dir=uploads/

# Multipart / File Upload
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=20MB
spring.servlet.multipart.max-request-size=100MB

# Tomcat-level safety net (must be >= max-request-size)
server.tomcat.max-swallow-size=100MB
server.tomcat.max-http-form-post-size=100MB

# ===============================
# SMTP
# ===============================
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=yashraj.gmtechnosys@gmail.com
spring.mail.password=fxqa szwb aolk svtp
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000